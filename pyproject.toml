[project]
name = "claude-usage"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "curl-cffi>=0.14.0",
    "rookiepy>=0.5.6",
    "rumps>=0.4.0",
]
