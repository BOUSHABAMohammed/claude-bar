rumps==0.4.0          # pinned: we use _menuitem private API that breaks across versions
rookiepy==0.5.6       # pinned: has direct Keychain access — supply chain risk if unpinned
curl_cffi==0.14.0
pyobjc-framework-Cocoa==12.1
